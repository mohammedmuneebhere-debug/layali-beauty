# Order transactional emails (confirmation + delivered)
# Pick ONE provider and add to .env.local / Vercel:

# Option A — Resend (recommended on Vercel)
# RESEND_API_KEY=re_xxxx
# EMAIL_FROM="Layali <onboarding@resend.dev>"

# Option B — SMTP (e.g. same Gmail used for Supabase OTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=layalibeautystore@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM="Layali <layalibeautystore@gmail.com>"
